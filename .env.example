OPENAI_API_KEY=sk-replace-me
ANTHROPIC_API_KEY=sk-ant-replace-me

# Optional: a Slack incoming webhook URL. When set, `drift run` posts a
# message here whenever drift is detected. Leave blank to disable alerts.
SLACK_WEBHOOK_URL=
